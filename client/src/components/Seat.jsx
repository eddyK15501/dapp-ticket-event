/* eslint-disable no-unused-vars */
/* eslint-disable react/prop-types */
import React from 'react';

const Seat = ({
  i,
  step,
  columnStart,
  maxColumns,
  rowStart,
  maxRows,
  seatTaken,
  onBuyHandle,
}) => {
  return <div>Seat</div>;
};

export default Seat;
